# Couche 1 — HMM
hmmlearn>=0.3        # GaussianHMM (fit / predict_proba)
scipy>=1.10          # densités gaussiennes (posterior d'émission)
scikit-learn>=1.2

# Données + calcul
yfinance>=0.2        # téléchargement des prix (repli synthétique si indisponible)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7

# Notebook
jupyter

# Optionnel — version "bayésienne" du HMM (cf. PyMC-HMM-Trading-Alpha.ipynb)
# pymc>=5.0
# arviz>=0.14
